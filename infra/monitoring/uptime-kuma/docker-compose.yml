version: '3.8'

services:
  uptime-kuma:
    image: louislam/uptime-kuma:latest
    container_name: cuentax-uptime-kuma
    restart: always
    ports:
      - "3001:3001"
    volumes:
      - uptime_data:/app/data
    environment:
      - NODE_ENV=production

volumes:
  uptime_data:
